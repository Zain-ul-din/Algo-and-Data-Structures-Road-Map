
#include "Time.h"

int main () {
    Time time1;
    cout<<time1;

}


/*
 * Node
 * if(Node== null) return
 *  Node == head : head = head->next
 *  Node* itr = head;
 *  while(itr != nullptr)
 *     if(itr->next->data == data)
 *            itr->next = itr->next->next;
 *            return;
 *
 */

